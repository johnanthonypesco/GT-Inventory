@props(['id' => '', 'btnType' => 'button'])

<button id="{{$id}}" type="{{ $btnType }}" class="mt-10 flex items-center gap-2 shadow-sm shadow-blue-500 px-5 py-2 rounded-lg cursor-pointer"><img src="{{asset('image/image 51.png')}}">Submit</button>

